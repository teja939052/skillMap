{
  "name": "skill-map",
  "version": "1.0.0",
  "private": true,
  "description": "Skill Map — Academia × Industry Competency Intelligence Platform (SIH26044)",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:worker": "npm run dev --prefix backend/worker",
    "build": "npm run build --prefix shared/config && npm run build --prefix shared/contracts && npm run build --prefix shared/utils && npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm ci --prefix shared/config && npm ci --prefix shared/contracts && npm ci --prefix shared/utils && npm ci --prefix backend && npm run build --prefix shared/config && npm run build --prefix shared/contracts && npm run build --prefix shared/utils && npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:render": "npm ci --prefix shared/config && npm ci --prefix shared/contracts && npm ci --prefix shared/utils && npm ci --prefix backend --include=dev && npm run build --prefix shared/config && npm run build --prefix shared/contracts && npm run build --prefix shared/utils && node backend/build.js",
    "build:vercel": "npm ci --prefix shared/contracts && npm ci --prefix shared/config && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
