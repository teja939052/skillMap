{
  "buildCommand": "npm ci --prefix shared/contracts && npm ci --prefix shared/config && npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm ci --prefix frontend",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
